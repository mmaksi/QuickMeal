import {
  AccountBackground,
  AccountCover,
} from "@/features/account/components/account.styles";

export const LoginScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
    </AccountBackground>
  );
};
